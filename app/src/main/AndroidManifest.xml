<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iwallic.app">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="19" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/ThemeDefault">
        <activity
            android:name=".base.WelcomeActivity"
            android:theme="@style/PageFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".base.MainActivity" />
        <activity android:name=".user.UserSettingActivity" />
        <activity android:name=".user.UserSettingLanguageActivity" />
        <activity android:name=".user.UserSettingThemeActivity" />
        <activity android:name=".wallet.WalletActivity" />
        <activity android:name=".wallet.WalletCreateActivity" />
        <activity android:name=".wallet.WalletImportActivity" />
        <activity android:name=".wallet.WalletBackupActivity" />
        <activity android:name=".wallet.WalletVerifyActivity" />
        <activity android:name=".transaction.TransactionTransferActivity" />
        <activity android:name=".transaction.TransactionDetailActivity" />
        <activity android:name=".asset.AssetDetailActivity" />
        <activity android:name=".asset.AssetManageActivity" />
        <activity android:name=".transaction.TransactionReceiveActivity" />

        <service
            android:name=".services.BlockIntentService"
            android:exported="false"/>
    </application>

</manifest>
