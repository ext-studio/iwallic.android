package com.iwallic.app.utils

/**
 * Use Models.TransactionModel instead
 * 1. get utxo list of asset to address (if NEP5 skip)
 * 2. call static function in TransactionModel to create new tx
 * 3. sign tx by sign() method in tx data
 * 4. send serialized raw transaction
 */

object TransactionUtils {

}
